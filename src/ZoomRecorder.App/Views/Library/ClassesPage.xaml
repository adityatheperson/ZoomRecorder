<Page
    x:Class="ZoomRecorder.App.Views.Library.ClassesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:libraryVm="using:ZoomRecorder.App.ViewModels.Library">
    <ScrollViewer>
        <StackPanel Padding="36" Spacing="20" MaxWidth="1100" HorizontalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel Spacing="4">
                    <TextBlock Text="Classes" Style="{ThemeResource TitleTextBlockStyle}" />
                    <TextBlock Text="Lectures stay local and are grouped by class." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <Button Grid.Column="1" Content="Record a class" Style="{ThemeResource AccentButtonStyle}" Click="RecordClassClicked" />
            </Grid>

            <StackPanel x:Name="LoadingPanel" Orientation="Horizontal" Spacing="10">
                <ProgressRing IsActive="True" Width="20" Height="20" />
                <TextBlock Text="Loading classes…" VerticalAlignment="Center" />
            </StackPanel>

            <Border x:Name="UnavailablePanel" Padding="18" CornerRadius="10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Visibility="Collapsed">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="UnavailableText" Text="The class library is unavailable right now. You can still record a class." TextWrapping="Wrap" />
                    <Button Content="Retry library" HorizontalAlignment="Left" Click="RetryClicked" />
                </StackPanel>
            </Border>

            <StackPanel x:Name="LibraryContent" Spacing="22" Visibility="Collapsed">
                <StackPanel x:Name="EmptyClassesPanel" Spacing="8">
                    <TextBlock Text="No classes yet" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Record a class or assign an existing recording to create your first class." TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Record a class" Style="{ThemeResource AccentButtonStyle}" Click="RecordClassClicked" />
                        <Button Content="Assign a recording" Click="ReviewRecordingsClicked" />
                    </StackPanel>
                </StackPanel>

                <ListView x:Name="ClassesList" ItemsSource="{Binding Classes}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="libraryVm:ClassCardViewModel">
                            <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Click="ClassClicked">
                                <Grid Padding="10" ColumnSpacing="18">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="{x:Bind Name}" FontSize="20" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind Term}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{x:Bind MostRecentLectureText}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" Spacing="4">
                                        <TextBlock Text="{x:Bind LectureCount}" HorizontalAlignment="Right" />
                                        <TextBlock Text="lectures" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{x:Bind StudyPackageStatus}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Spacing="8">
                    <TextBlock Text="Unassigned recordings" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <TextBlock Text="{Binding UnassignedCount}" />
                        <TextBlock Text="need a class" />
                    </StackPanel>
                    <Button Content="Review and assign" HorizontalAlignment="Left" Click="ReviewRecordingsClicked" />
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock Text="Recent recordings" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <ListView ItemsSource="{Binding RecentRecordings}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="libraryVm:RecordingListItem">
                                <Grid Padding="10" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBlock Text="{x:Bind FileName}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind RecordedAtText}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
